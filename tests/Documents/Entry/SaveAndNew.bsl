// Create Entry
// Press Save and New

Call ( "Common.Init" );

// Create Entry
Commando ( "e1cib/data/Document.Entry" );
With ( "Entry (cr*" );
Click ( "#FormWrite" );
Click ( "#FormCommonCommandSaveAndNew" );
